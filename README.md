# laravel-school-api-hateoas
Laravel School Management REST API with Collection+JSON Implementation. Manages teachers, students, classes, subjects, and schedules with standardized REST collections.  Features: - Collection+JSON format - Hypermedia support (HATEOAS) - CRUD operations for all resources - Self-documenting API with templates - Query templates for searching
